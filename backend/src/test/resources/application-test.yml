spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: never

logging:
  level:
    root: WARN
    com.rathore.stockanalysis: DEBUG

stockanalysis:
  exchange:
    default: NSE
  schema:
    nse:
      master: nse_exchange_symbol
      market: market_nse
      core-app: core_app_nse
