spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/stock_analysis}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

logging:
  level:
    org.springframework.jdbc.core: DEBUG
    com.rathore.stockanalysis: DEBUG
